name: dependency-review

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Diffs the dependency graph of the PR branch against the base branch
      # and fails on any newly added package with a known advisory. This is
      # what makes a vulnerable dependency block the PR itself; Dependabot
      # alerts only ever scan the default branch after a merge.
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: low
          comment-summary-in-pr: on-failure
